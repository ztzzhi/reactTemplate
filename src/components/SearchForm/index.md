---
title: SearchForm
order: 2
group:
  title: 业务组件
  path: /components/table
---

# SearchForm

## 功能特性

- 🖥️ 响应式布局（支持 6 种屏幕尺寸适配）
- 🪄 智能折叠功能（默认展示前 2 项）
- 📱 窗口大小自适应（自动调整布局）
- 🧩 支持 20+ 表单控件类型
- 🔄 内置防抖处理（窗口 resize 事件）
- 🎨 可扩展的表格操作区
- ⚡ 自动聚焦首项输入框

## 基本用法

```tsx
import Demo from './demo'
export default () => <Demo></Demo>
```

---

# Props 参数说明

## 基础配置

| 参数           | 类型               | 默认值  | 说明                 |
| -------------- | ------------------ | ------- | -------------------- |
| `searchConfig` | `IItemArray[]`     | 必填    | 表单项配置数组       |
| `isFold`       | `boolean`          | `false` | 初始折叠状态         |
| `onFinish`     | `(values) => void` | 必填    | 表单提交回调         |
| `onReset`      | `() => void`       | 必填    | 重置按钮回调         |
| `extra`        | `ReactNode`        | `-`     | 表格操作区           |
| `className`    | `string`           | `-`     | 外层容器类名         |
| `form`         | `FormInstance`     | `-`     | 可传入外部 form 实例 |

---

## 表单项配置 (IItemArray)

| 参数          | 类型                                             | 说明                       |
| ------------- | ------------------------------------------------ | -------------------------- |
| `type`        | `FormItemType`                                   | 控件类型（见下方支持类型） |
| `label`       | `string`                                         | 表单项标签                 |
| `name`        | `string`                                         | 表单字段名                 |
| `placeholder` | `string`                                         | 自定义占位符               |
| `hidden`      | `boolean`                                        | 是否隐藏该项               |
| `option`      | `{label:string\|number, value:string\|number}[]` | 选择器选项数据             |
| `config`      | `object`                                         | 控件原生 props             |
| `slot`        | `ReactNode`                                      | 自定义组件插槽             |

---

# 支持的控件类型

```ts
type FormItemType =
  | 'Input'
  | 'Search'
  | 'Password'
  | 'TextArea'
  | 'Select'
  | 'TreeSelect'
  | 'AutoComplete'
  | 'DatePicker'
  | 'RangePicker'
  | 'FixedRangePicker'
  | 'FixedDatePicker'
  | 'Cascader'
  | 'Checkbox'
  | 'Radio'
  | 'Switch'
  | 'InputNumber'
  | 'Upload'
  | 'UploadFile'
  | 'Transfer'
  | 'Custom' // 自定义组件类型
```

## 响应式断点

| 屏幕尺寸   | 布局方式 |
| ---------- | -------- |
| <768px     | 单列布局 |
| 768-992px  | 双列布局 |
| 992-1600px | 三列布局 |
| ≥1600px    | 四列布局 |

---

# 实现原理说明

SearchForm 组件基于 Ant Design 的 Form 体系，结合自定义配置实现灵活的表单渲染。其核心原理如下：

1. **响应式布局**：通过监听容器宽度（使用 ResizeObserver），动态调整每行可容纳的表单项数量（currentSpan），实现 6 种屏幕尺寸的自适应布局。
2. **智能折叠**：支持初始折叠（isFold）和"更多筛选"功能，默认仅展示前两项，用户可展开查看更多筛选项。
3. **动态表单渲染**：根据传入的 searchConfig 配置数组，自动渲染不同类型的表单控件（支持 20+ 类型），并支持隐藏、插槽、自定义 props 等扩展。
4. **表单交互**：内置查询、重置按钮，支持外部 form 实例、初始值、onFinish/onReset/onValuesChange 等事件。
5. **操作区扩展**：通过 extra 属性可自定义表格操作区内容，增强灵活性。
6. **主题与国际化**：内置主题色、圆角、边框等样式定制，并支持中文本地化。

整体设计注重易用性、扩展性和良好的用户体验，适用于多场景的业务搜索表单开发。
